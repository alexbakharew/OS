CLIENT
При запуске происхдит идентификация пользователя. Вводится его имя - это и будет адресом клиента. Создается сокет данного пользователя и к нему прикрепляется заданный адресс. Инициализируется сервер(Адресс сервера вынесен в config.h). Затем происходит подключение к серверу.

SERVER

