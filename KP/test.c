#include <string.h>
#include "config.h"
#include <stdio.h>
int main()
{
    char str1[] = {"string1"};
    //int i = 50;
    char i[] = {"JOPA"};
    strcat(str1, i);
    //printf("%s\n", str1);
    return 0;
}